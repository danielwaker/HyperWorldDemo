using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnterCollide : MonoBehaviour
{

    // Update is called once per frame
    void Update()
    {
        /*Vector3 delta = WCollider.Collide(Vector3.zero, GetComponent<WarpCollider>().boundingBoxes[0].size.x / 2, out Vector3 sinY, false, -GetComponent<HyperObject>().localGV, "Tower");
        if (delta.sqrMagnitude > 0.0f)
        {
            print(transform.name + " has been collided with");
        }*/
    }
}
